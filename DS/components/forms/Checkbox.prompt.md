Labeled checkbox; set `indeterminate` for mixed parent state. Checked fill is the brand accent (copper).

```jsx
<Checkbox label="Include retired assets" defaultChecked />
<Checkbox label="Some selected" indeterminate />
```