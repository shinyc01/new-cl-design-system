Native `<select>` styled to the system with a custom chevron.

```jsx
<Select label="Scenario" options={["Baseline","Optimized","Constrained"]} defaultValue="Optimized" />
```

`options` accepts strings or {value,label}. Supports `placeholder`, `size`.