Horizontal workflow stepper for multi-stage pipelines (Input → Shape → Review → Publish).

```jsx
<Stepper current={2} steps={["Input","Shape","Review","Publish"]} />
```

Steps before `current` show a copper check; `current` is active.